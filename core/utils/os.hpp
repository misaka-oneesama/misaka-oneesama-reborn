#ifndef UTILS_OS_HPP
#define UTILS_OS_HPP

#include <string>

namespace Utils
{
    /**
     * Get the operating system name at runtime.
     */
    const std::string get_os_name();
}

#endif // UTILS_OS_HPP
